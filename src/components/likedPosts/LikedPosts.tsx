import React from 'react';
import classes from './LikedPosts.module.css';

interface Props {
    title: string;
}

const LikedPosts: React.FC<Props> = ({ title }) => {
    return (
        <div className={classes.container}>
        
            
        </div>
    );
};

export default LikedPosts;